const { search } = require('');
const { sort } = require('../../../current-work/week-of-test/ds-and-algo-practice/testsort');
const { treeTraversal } = require('');
const { graphTraversal } = require('');

module.exports = {
    search: search,
    sort: sort,
    treeTraversal: treeTraversal,
    graphTraversal: graphTraversal,
};